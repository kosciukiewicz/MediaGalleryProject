<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/singlePageContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    android:padding="16dp"
    android:gravity="center">

    <ImageView
        android:id="@+id/singlePageImageView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="64dp"
        android:src="@drawable/sample_image_1" />

    <TextView
        android:id="@+id/singlePageTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:ellipsize="end"
        android:maxLines="3"
        android:paddingEnd="32dp"
        android:paddingStart="32dp"
        android:text="asdasd"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/singlePageTextViewDateDone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/singlePageTextView"
        android:layout_centerHorizontal="true"
        android:ellipsize="end"
        android:paddingEnd="16dp"
        android:paddingStart="16dp"
        android:text="asdasd"
        android:textColor="@color/white"
        android:textSize="14sp" />
</RelativeLayout>